# somador
# o usuario digita o numero para qual ele deseja somar pode ser usado int ou float
n1 = int(input('digite um numero: '))
# o usuario digit o segundo numero para terminar o calculo assim como a linha de cima
n2 = int(input('digite outro numero: '))
# o programa soma os dois valores
soma = n1 + n2
# o programa mostra o resultado
# o print "print('A soma entre',n1,'e',n2,'vale {}'.format(soma))"
# pode ser substituído pelo exemplo a seguir
print('A soma de {} + {} = {}.'.format(n1, n2 , soma))
